{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean -y",
    "test": "lerna run test",
    "test:cover": "jest --coverage",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "coveralls": "^3.0.5",
    "jest": "^24.8.0",
    "lerna": "^3.13.2",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  }
}